<h1>商品新規登録</h1>
  <%= form_with model: @shop_image do |f| %>
    <h4>商品画像</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>商品名</h4>
      <%= f.text_field :shop_name %>
    <h4>商品説明</h4>
      <%= f.text_area :caption %>
    <%= f.submit '新規登録' %>
  <% end %>